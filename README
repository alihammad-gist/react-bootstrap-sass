### Twitter Bootstrap SASS + ReactJS

This is a boilerplate for building reactjs 

## Installation 

Navigate to the directory where you clone this repo. And run these commands

`npm install -g gulp bower`
`npm install`
`bower install`

## Build Script

This repo include few gulp tasks that would compile your sass source 